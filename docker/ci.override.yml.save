services:
  uwsgi:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: intelowlproject/intelowl:ci
    env_file:
      - env_file_app_ci

  daphne:
    build:
      context: ..
      dockerfile: docker/Dockerfi
