export const DecayProgressionTypes = Object.freeze({
  LINEAR: 0,
  INVERSE_EXPONENTIAL: 1,
  FIXED: 2,
});

export const DecayProgressionDescription = Object.freeze({
  0: "linear decay",
  1: "inverse exponential decay",
  2: "Fixed decay",
});
