{{- if .Values.storage.localStorage }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "intelowl.fullname" . }}-generic-logs
  labels:
    {{- include "intelowl.labels" . | nindent 4 }}
    app.kubernetes.io/component: storage
  {{- if .Values.migration.enabled }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-15"
    "helm.sh/resource-policy": keep
  {{- end }}
spec:
  accessModes:
    {{- range .Values.storage.genericLogs.accessModes }}
    - {{ . }}
    {{- end }}
  resources:
    requests:
      storage: {{ .Values.storage.genericLogs.size }}
  {{- include "intelowl.storageClass" (dict "storageClass" .Values.storage.genericLogs.storageClass "Values" .Values) | nindent 2 }}
{{- end }}
